// Observed Predicates

*parent(Person, Person)

// Hidden Predicates

hates(Person,Person)
loves(Person, Person)

conf: !lovesDegree(x, y, conf) v loves(x, y)

// ? !loves(x,y) v hates([parent(?,x)],[parent(?,y)])